{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://saripalana.github.io/ks-study-guide/schemas/question-bank.schema.json",
  "title": "Psychiatry Board Question Bank Package",
  "type": "object",
  "required": ["schemaVersion", "projectId", "datasetId", "questionCount", "bankHash", "questions"],
  "properties": {
    "schemaVersion": { "type": "integer", "minimum": 1 },
    "projectId": { "const": "psychiatry-board-practice" },
    "datasetId": { "const": "psychiatry-board-question-bank" },
    "questionCount": { "type": "integer", "minimum": 1 },
    "bankHash": { "type": "string", "minLength": 1 },
    "questions": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "chapter", "chapterTitle", "qnum", "question", "choices", "choiceLetters", "correctLetter", "answerText", "explanation"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "chapter": { "type": "integer", "minimum": 1 },
          "chapterTitle": { "type": "string" },
          "qnum": { "type": "integer", "minimum": 1 },
          "question": { "type": "string", "minLength": 1 },
          "choices": { "type": "array", "minItems": 2, "items": { "type": "string" } },
          "choiceLetters": { "type": "array", "minItems": 2, "items": { "type": "string", "minLength": 1 } },
          "correctLetter": { "type": "string", "minLength": 1 },
          "answerText": { "type": "string" },
          "explanation": { "type": "string" }
        },
        "additionalProperties": true
      }
    }
  },
  "additionalProperties": true
}